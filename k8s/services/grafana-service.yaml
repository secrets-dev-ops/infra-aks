apiVersion: v1
kind: Service
metadata:
  name: grafana-service
  namespace: metrics
  labels:
    app: grafana
spec:
  type: ClusterIP 
  ports:
    - name: web
      protocol: TCP
      port: 80
      targetPort: 3008
  selector:
      app: grafana